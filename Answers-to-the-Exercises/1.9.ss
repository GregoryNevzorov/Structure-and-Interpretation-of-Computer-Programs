#lang racket

(define (+A a b)
  (if (= a 0)
      b
      (add1 (+A (sub1 a) b))))

(+A 4 5) ;9
;Рекурсивная процедура, рекурсивный процесс

(define (+B a b)
  (if (= a 0)
      b
      (+B (sub1 a)(add1 b))))

(+B 4 5) ;9
;Рекурсивная процедура, итеративный процесс
